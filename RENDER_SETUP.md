# BookedRadar Render setup

Status: locally verified; not uploaded or deployed. GitHub rejected the connector write with HTTP 403 (Resource not accessible by integration).

1. Extract this ZIP on your computer.
2. Open https://github.com/allenrandy74-dev/Bookedradar-platform-/upload/main and upload the extracted files and folders, not the ZIP. Ensure package.json, package-lock.json, server.js, Dockerfile and render.yaml are at the repository root. Commit the upload.
3. Open https://dashboard.render.com/blueprint/new?repo=https://github.com/allenrandy74-dev/Bookedradar-platform- and choose My Workspace. Connect GitHub if requested.
4. Review the paid Starter service and 1 GB persistent disk cost displayed by Render, then deploy.
5. After the deployment is live, check /health. Admin and ingest tokens are generated by Render; retrieve them only from the service Environment page when needed.

The persistent disk retains lead and recovery records across restarts. Do not remove it for production use. Automatic deployments are off.

Voice and all demo customer integrations remain disabled. This deployment does not activate phone answering, SMS, email, CRM, or live booking. Configure real customer credentials and numbers and complete end-to-end testing before onboarding customers. Never commit .env or credentials.

Verified: 28 tests passed, JavaScript syntax checks passed, tenant configuration passed, clean dependency installation passed, and local health/intake/RadarProof smoke test passed. Render-side deployment and Blueprint validation have not yet run.
